name: Build and test

on: push

jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@3
        
      - name: Build package
        run: yarn build
        
      - name: Test the package
        run: yarn test
